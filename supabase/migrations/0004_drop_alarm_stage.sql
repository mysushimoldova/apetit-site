-- 0004_drop_alarm_stage.sql — убирает колонку orders.alarm_stage.
-- Применяется вручную: Supabase → SQL Editor → вставить → Run. Повторный
-- запуск безопасен. Требует 0003_alerts.sql.
--
-- Колонка осталась от промежуточной схемы напоминаний (серии тревог 🚨).
-- После перехода на две дорожки (reminders_sent / owner_alerts_sent) её
-- никто не читает и не пишет: ни сайт, ни функции базы. Решение архитектора
-- 22.09.2026 — убрать отдельной миграцией, не ждать правок схемы под админку.
-- Данных в ней нет: везде 0 по умолчанию.

alter table public.orders
  drop column if exists alarm_stage;
