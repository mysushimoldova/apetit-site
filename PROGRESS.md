# PROGRESS.md — журнал работ по сайту apetit.md

Новые записи добавляются в конец. Каждая запись: дата, этап, что сделано,
что не получилось, что нужно от Амяна. Этот файл Амян отправляет архитектору
после каждой задачи.

---

## Фото на пересъёмку (постоянный список)

Обновляется по мере работы. Пока фото нет или оно временное, на сайте стоит
то, что есть (или заглушка с рисованной тарелкой).

| Что | Почему | С какого числа |
|---|---|---|
| `brinzoaice` | вырезка без коробочки (решение архитектора — оставить до нового фото) | 19.09.2026 |
| `hot-dog-classic`, `hot-dog-cheese` | временные фото (SPEC 2.7) | 19.09.2026 |
| все `sos-*` (7 шт.) | временные фото (SPEC 2.7) | 19.09.2026 |
| лимонады: `limonada-aloe-fresh`, `limonada-portocala` | фото нет — на сайте заглушка | 19.09.2026 |
| чай и кофе: `ceai-craft`, `espresso`, `americano`, `latte`, `cappuccino` | фото нет — на сайте заглушка (решение архитектора) | 19.09.2026 |

Имя нового файла = slug из таблицы, положить в `assets/foto-originale/` и
запустить `npm run images`.

---

## 2026-09-19 — Этап 4 — настройка проекта

### Проверка инструментов

| Инструмент | Статус |
|---|---|
| git | 2.55.0 — есть |
| Claude Code | 2.1.266 — есть |
| Node.js / npm | **НЕТ** — не установлен |
| Python | **НЕТ** — не установлен (есть только пустая заглушка Microsoft Store) |

### Что сделано
- Подключён MCP-сервер **Context7** (актуальная документация библиотек):
  `claude mcp add --transport http context7 https://mcp.context7.com/mcp`.
  Проверка `claude mcp list` — статус Connected. Конфиг лежит локально
  в `~/.claude.json` для этого проекта, в git не попадает.
- В репозиторий добавлены `CLAUDE.md`, `SPEC.md`, `DESIGN.md` (дизайн-система),
  `docs/fon-variante.png` и этот журнал.

### Что не встало
**Ни один скилл не установлен.** Все команды `npx skills add …` требуют
Node.js, а его на компьютере нет. Пропущены все 17 команд:

- `anthropics/skills`: frontend-design, webapp-testing
- `nextlevelbuilder/ui-ux-pro-max-skill`: ui-ux-pro-max
- `emilkowalski/skills`: emil-design-eng, animate, improve-animations,
  review-animations, apple-design
- `vercel-labs/agent-skills`: web-design-guidelines, react-best-practices,
  composition-patterns
- `obra/superpowers` (весь набор)
- `mattpocock/skills`: grill-me, code-review, diagnosing-bugs,
  git-guardrails-claude-code, handoff

Папка `.claude/skills/` пока не создана. Коммит сделан без скиллов, поэтому
сообщение коммита отличается от запланированного («add project skills…»):
`chore: add project docs, design system and progress log`.

### Что нужно от Амяна
1. **Установить Node.js LTS** — https://nodejs.org/ (кнопка «Download Node.js (LTS)»,
   установщик для Windows). npm ставится вместе с ним. Без Node.js не заработает
   ни установка скиллов, ни сам Next.js. После установки перезапустить VS Code.
2. **Установить Python 3** — https://www.python.org/downloads/ (кнопка «Download
   Python 3.x»). При установке обязательно поставить галочку **«Add python.exe
   to PATH»**. Нужен для скиллов `ui-ux-pro-max` (поиск по базе дизайн-правил)
   и `webapp-testing` (скрипты Playwright). Если после установки команда
   `python --version` по-прежнему открывает Microsoft Store — выключить
   псевдоним: Параметры → Приложения → Дополнительные параметры приложений →
   Псевдонимы выполнения приложений → отключить `python.exe` и `python3.exe`.
3. После установки повторить шаг 2 этапа 4 (все команды `npx skills add …`,
   агент — Claude Code) и сделать коммит `chore: add project skills`.

### Вопросы
- Нет.

---

## 2026-09-19 — Этап 4 — установка скиллов (повтор шага 2)

### Проверка инструментов

| Инструмент | Статус |
|---|---|
| Node.js | v24.21.0 — есть |
| npm | 11.19.0 — есть |
| Python | 3.14.7 — есть (`py --version` и `python --version` работают, Store не открывается) |
| gh (GitHub CLI) | 2.101.0 — есть, вход под mysushimoldova |

Замечание: Node.js прописан в системном PATH, но окно VS Code, из которого
шла работа, было открыто до его установки и Node не видело. В этой сессии путь
добавлялся вручную; после перезапуска VS Code всё работает само.

### Что сделано
- Установлены все скиллы в папку проекта `.claude/skills/` (агент — Claude Code,
  не глобально). Итого 31 папка:
  - `anthropics/skills`: frontend-design, webapp-testing
  - `nextlevelbuilder/ui-ux-pro-max-skill`: ui-ux-pro-max
  - `emilkowalski/skills`: emil-design-eng, animate, improve-animations,
    review-animations, apple-design
  - `vercel-labs/agent-skills`: web-design-guidelines,
    vercel-react-best-practices, vercel-composition-patterns
  - `obra/superpowers` (весь набор, 15 скиллов): brainstorming,
    diagnosing-superpowers, dispatching-parallel-agents, executing-plans,
    finishing-a-development-branch, receiving-code-review,
    requesting-code-review, subagent-driven-development, systematic-debugging,
    test-driven-development, using-git-worktrees, using-superpowers,
    verification-before-completion, writing-plans, writing-skills
  - `mattpocock/skills`: grill-me, code-review, diagnosing-bugs,
    git-guardrails-claude-code, handoff
- В корне появился `skills-lock.json` — его создаёт `npx skills`, он фиксирует,
  откуда и какой версии каждый скилл. Закоммичен вместе со скиллами.
- Проверено, что `ui-ux-pro-max` запускается на Python 3.14
  (`py -3 scripts/search.py "fast food restaurant" --design-system` выдаёт результат).
- Выполнен `gh auth setup-git`: для github.com git теперь берёт токен из
  GitHub CLI, а не из Git Credential Manager (который открывал окно и вешал
  `git push`). Настройка в `~/.gitconfig`, в репозиторий не попадает.

### Что не встало / отличия от задания
- В `vercel-labs/agent-skills` скиллов с именами `react-best-practices` и
  `composition-patterns` больше нет — в репозитории их переименовали в
  `vercel-react-best-practices` и `vercel-composition-patterns`. Установлены
  под новыми именами (репозиторий тот же, содержимое то же). В CLAUDE.md
  упоминается только `web-design-guidelines`, поэтому правки там не нужны.
- Всё остальное встало с первого раза.

### Что нужно от Амяна
1. Перезапустить VS Code (закрыть полностью и открыть заново), чтобы новое
   окно видело Node.js без ручного добавления пути.
2. Ничего больше не требуется.

### Вопросы
- Нет.

---

## 2026-09-19 — Этап 5 — каркас проекта

### Что сделано
- Создан проект **Next.js 15.5.25** (App Router, TypeScript strict, `src/`,
  alias `@/`), сборщик Turbopack. Менеджер пакетов — npm. Проект создан
  командой `create-next-app@15` во временной папке и перенесён сюда, потому что
  CLI отказывается работать в непустой папке. Существующие файлы не тронуты.
- **Tailwind CSS 4.3.3.** Вся дизайн-система из DESIGN.md переведена в
  `src/app/globals.css`:
  - цвета (Cream, Milk, Sand, Yellow, Yellow Deep, Ink, Charcoal, Smoke, Ash,
    Open, Closed, Peach Glow) — как CSS-переменные и утилиты Tailwind
    (`bg-cream`, `text-ink`, `bg-yellow`…). Стандартные цвета Tailwind
    (white, gray, purple…) **отключены** — по DESIGN.md чистый белый/серый
    запрещены, так их просто нельзя случайно использовать;
  - радиусы (`rounded-pill`, `rounded-card`, `rounded-tile`, `rounded-sheet`,
    `rounded-input`, `rounded-image`, `rounded-badge`), тени (`shadow-card`,
    `shadow-lift`, `shadow-glass`), кривая `ease-out`, длительности
    `--dur-fast/base/slow` (при `prefers-reduced-motion` = 0), переменные
    стекла и фоновых пятен;
  - шкала текста из таблицы Type Scale: `text-caption … text-city` — каждая
    сразу задаёт размер, межстрочный интервал и жирность;
  - утилиты `page` (контейнер 1200px, отступы 16px на телефоне / 40px с 1024px)
    и `glass` (стекло с запасным вариантом без blur).
- **Шрифты** — `src/lib/fonts.ts`: Oswald 600, Manrope 600/700/800,
  Montserrat 400/500, subsets latin + latin-ext + cyrillic, через
  `next/font/google`. Проверено: при сборке 16 файлов woff2 легли в
  `.next/static/media`, в HTML нет ни одной ссылки на Google — с CDN ничего
  не грузится.
- **Библиотеки** (только три, все из утверждённого стека):
  - `motion` 13.4.0 — анимации из SPEC 6.3 (появление городов, карточек,
    корзина). Уважает reduced-motion, лёгкая.
  - `lucide-react` 1.47.0 — иконки (DESIGN.md → Imagery: «Иконки — Lucide»).
  - `zod` 4.6.5 — валидация входных данных на сервере (CLAUDE.md, SPEC 9.4).
- **Инструменты качества:** Prettier 3.9.8 (форматирует только код, `.md`
  и документы не трогает), Vitest 5.0.1 (один тест: сторожит, что в токенах
  есть фирменный жёлтый и нет чистого белого/чёрного), Playwright 1.63.0
  (один smoke-тест: главная открывается на профиле телефона и содержит
  APETIT; браузер Chromium установлен). Скрипты: `dev`, `build`, `lint`,
  `test`, `test:e2e`, плюс `format` / `format:check`.
- **Временная главная:** на Cream слово APETIT (Oswald 44px), строка городов
  (Manrope 700), строка с румынскими ș ț ă â î (Montserrat 400) и русская
  строка (Montserrat 500). Скриншот на 390px проверен — все шрифты и символы
  отрисовываются.
- `.gitignore` дополнен для Next.js: node_modules, .next, .env*, test-results,
  playwright-report и т.д.
- Все проверки проходят: `npm run build` ✓, `npm run lint` ✓, `npm test`
  (2 теста) ✓, `npm run test:e2e` (1 тест) ✓.

### Что не встало / отличия от задания
- **Правка CLAUDE.md не понадобилась:** имён `react-best-practices` и
  `composition-patterns` в файле нет (это уже отмечалось в отчёте этапа 4).
  Файл не менял.
- **Context7 в этой сессии недоступен** как инструмент (MCP-сервер настроен
  в `~/.claude.json`, но в текущем окне Claude Code его нет в списке).
  Документацию Next.js 15 / next/font и Tailwind v4 / `@theme` проверил
  напрямую на nextjs.org и tailwindcss.com. Стоит проверить `claude mcp list`
  в новом окне.
- **Контейнер: DESIGN.md говорит 1120px, задача — 1200px.** Сделал 1200 как
  в задаче (`--container-page`). Если правильное значение 1120 — одна строка
  в `globals.css`, и DESIGN.md тогда уже верен.
- `@types/node` поднят с 20 до 24 (у нас Node 24, а Vitest 5 требует ≥22) —
  иначе установка падала с конфликтом.
- `npm audit` показывает 2 уязвимости в **postcss, который лежит внутри
  самого Next.js 15** (используется только при сборке, не в браузере).
  Единственное «лечение» по npm — переход на Next.js 16, что противоречит
  SPEC 9.1. Оставил как есть; вопрос архитектору ниже.
- Тексты на временной главной захардкожены (системы переводов ещё нет) —
  это временная страница, она целиком заменится экраном городов.
- Скиллы `superpowers` (using-superpowers) и проверка документации выполнены;
  `ui-ux-pro-max` / `web-design-guidelines` не запускал — интерфейса как
  такового на этом этапе нет, только токены и одна проверочная страница.

### Как запустить сайт и посмотреть в браузере (пошагово)
1. Открыть VS Code с папкой `C:\apetit-site`.
2. Открыть терминал: меню **Terminal → New Terminal** (или `Ctrl` + `` ` ``).
3. Ввести `npm run dev` и нажать Enter. Через несколько секунд появится
   строка вида `- Local: http://localhost:3000`.
4. Открыть в браузере адрес **http://localhost:3000** — это и есть сайт.
   Внизу слева будет маленький кружок с буквой N — это служебный значок
   режима разработки, на настоящем сайте его не будет.
5. Остановить сайт: щёлкнуть в терминал и нажать `Ctrl` + `C`.

Другие команды (тоже в терминале):
- `npm run build` — собрать сайт как для боевого сервера (проверка, что всё
  компилируется);
- `npm run lint` — проверка кода линтером;
- `npm test` — быстрые тесты; `npm run test:e2e` — тест в настоящем браузере
  (сам поднимает сайт и сам его гасит).

### Что нужно от Амяна
1. Ничего устанавливать не нужно — всё стоит. Один раз запустить `npm run dev`
   и посмотреть страницу — убедиться, что шрифты выглядят как в печатном меню.
2. Передать архитектору отчёт и вопросы ниже.

### Вопросы архитектору
1. Ширина контейнера: 1200px (задача) или 1120px (DESIGN.md)?
2. Уязвимости postcss внутри Next.js 15 (build-time). Оставляем Next 15 по
   SPEC или разрешаем Next 16? Если оставляем — предлагаю просто зафиксировать
   это в SPEC как известный и принятый риск.
3. Context7 не виден в этой сессии — проверить настройку MCP в новом окне?

---

## 2026-09-19 — Этап 5 — решения архитектора + экран выбора города

### Четыре решения архитектора

| Решение | Что сделано |
|---|---|
| a) Контейнер 1200px | В коде уже было 1200 (`--container-page`). В DESIGN.md стояло 1120 в двух местах — исправил на 1200, чтобы документ совпадал с решением. |
| b) Next.js 16 | **Обновились до Next.js 16.3.5.** Официальная документация OpenNext (opennext.js.org/cloudflare) говорит: «All minor and patch versions of Next.js 16 … are supported»; адаптер `@opennextjs/cloudflare` 1.20.6 требует `next >=16.3.3`. Заодно: React 19.3, `eslint-config-next` 16 (новый формат конфига без FlatCompat), флаг `--turbopack` убран из скриптов (в 16 он по умолчанию), `@eslint/eslintrc` больше не нужен — удалён. **`npm audit` теперь показывает 0 уязвимостей** — проблемный postcss уходил вместе с Next 15. Версия исправлена в CLAUDE.md («Стек») и SPEC.md (таблица 9.1). Next 16 сам поправил `tsconfig.json` (`jsx: react-jsx`, добавил `.next/dev/types`) и при каждом `next dev` дописывает в конец CLAUDE.md служебный блок `nextjs-agent-rules` (ссылка на встроенную документацию в `node_modules/next/dist/docs/`). По документации Next его нужно закоммитить — иначе он будет появляться как незакоммиченное изменение каждый раз. Закоммитил. |
| c) Context7 | `claude mcp list` показывает context7 как Connected (локальная настройка в `~/.claude.json`), но как инструмент в этой сессии он не виден. Добавил в scope проекта: `claude mcp add --transport http --scope project context7 …` → появился `.mcp.json` без секретов, закоммичен. Заработает после перезапуска окна Claude Code. Документацию Next 16 / OpenNext / Motion в этой сессии проверял напрямую на официальных сайтах. |
| d) `suppressHydrationWarning` | Добавлен на `<body>` в `src/app/layout.tsx` с комментарием, зачем. |

Коммит: `chore: upgrade to Next.js 16, add context7 to project scope`.

### Экран выбора города — что сделано

Процесс: план → «допрос плана» → тесты → код → проверка. План лежит в
`docs/superpowers/plans/2026-09-19-city-screen.md` (там же таблица решений
по спорным местам).

1. **Данные** — `src/data/points.ts`: 6 точек из SPEC 1.1 (город, slug,
   название, телефон, наша/франшиза, язык ro/ru, часы 08:30–23:00), адреса
   пустые с `TODO`. Плюс список из 5 городов в порядке SPEC и проверка slug.
2. **Маршрут `/`** — пять плиток: без заливки, рамка 1px Ink, радиус 16px,
   высота 88px, Oswald 44px заглавными по центру. Телефон — столбик, зазор
   12px, отступ 16px; десктоп (≥1024px) — ряд плиток по 200px по центру.
   Нажатие — заливка Ink, текст Cream, 150ms. Никакой шапки, подвала,
   логотипа, текста.
3. **Анимация входа** через motion: плитки снизу (y 24→0, opacity 0→1),
   задержка 60ms между плитками, кривая `--ease-out` и длительность
   `--dur-slow` (420ms) из DESIGN.md. При «уменьшить движение» — обычные
   ссылки без motion вообще.
4. **Маршрут `/[city]`** — заглушка: название города Oswald + `[ТЕКСТ: меню
   скоро]`. Страницы пяти городов статические; любой другой адрес
   (`/chisinau`) → 404.
5. **Запоминание.** Выбор пишется в `localStorage` (ключ `apetit.city`) при
   открытии страницы города — так работает и клик по плитке, и прямая ссылка
   из Google, и «открыть в новой вкладке». При следующем открытии `/` —
   редирект на сохранённый город **до загрузки React**: в начале страницы
   стоит крошечный скрипт (5 строк), который читает хранилище и уходит на
   `/soroca` раньше, чем экран успевает нарисоваться. Это стандартный приём
   (так же прячут мигание тёмной темы). Чужое значение в хранилище
   игнорируется. Функция «сменить город» — `useChangeCity()` в
   `src/lib/use-change-city.ts` (чистит выбор и ведёт на `/`); кнопка будет
   в шапке позже.
6. **Тесты.** Vitest — 23 теста (было 2): данные точек (6 точек, 5 городов,
   уникальные slug и телефоны, формат `0XXXXXXXX`, Otaci → ru, часы),
   хранилище (мусор → игнор, исключения Safari → не падает), скрипт редиректа
   (исполняется по-настоящему с подменёнными `localStorage`/`location`).
   Playwright на 390px — 8 тестов: 5 плиток и никакого другого текста; ссылки
   ведут на `/[city]`; клик по Soroca → `/soroca`; повторное открытие `/` →
   `/soroca`; мусор в хранилище не ломает главную; reduced-motion без
   анимации и без ошибок гидратации; страница города; неизвестный город → 404.
   Ревью по `web-design-guidelines` — два замечания исправлены
   (`touch-action: manipulation`, `translate="no"` на названиях городов).
7. **Скриншоты** — `docs/screens/01-orase-390.png` и `01-orase-1280.png`.
   Команда `npm run screens` (отдельный конфиг Playwright, в обычные тесты
   не входит).

Проверки: `npm run lint` ✓ · `npm run build` ✓ · `npm test` 23/23 ✓ ·
`npm run test:e2e` 8/8 ✓.

### Что не встало / отличия от задания

- **DESIGN.md в репозитории не совпадает с задачей.** В задаче сказано
  «строго по DESIGN.md „City Screen“», но в нашем DESIGN.md такого раздела
  нет — есть «City Tile» с другими значениями: Milk-заливка, радиус 24px,
  тень, высота 96px, шесть плиток, нажатие — scale 1.02. Сделал по цифрам из
  задачи (рамка Ink, 16px, 88px, пять плиток, нажатие — заливка Ink). Токен
  `--radius-tile` (24px) не трогал, 16px задан прямо в стиле плитки. **Нужен
  обновлённый DESIGN.md от архитектора**, чтобы документ и код совпадали.
- **Скилл `grill-me` не запускается:** его файл говорит «вызови скилл
  `grilling`», а такого скилла в проекте нет (пакет mattpocock/skills
  установлен не полностью). «Допрос плана» провёл сам — вопросы и решения
  записаны в плане.
- **Playwright переведён на один воркер.** Dev-сервер Next 16 при
  одновременных первых запросах к ещё не скомпилированной странице
  `/[city]` иногда отвечает 500 (`Unexpected end of JSON input` внутри
  Next, воспроизвёл через curl). Последовательно — всегда стабильно. На
  собранном сайте страницы статические, там этой гонки нет.
- **`useReducedMotion` из motion не использую:** он читает настройку уже в
  первом рендере и у людей с включённым «уменьшить движение» давал
  несовпадение HTML сервера и клиента (поймал тестом). Написал свой хук на
  15 строк через `useSyncExternalStore` — на сервере всегда «нет», на
  клиенте — настоящее значение.
- Длительность входа 420ms взята из токена `--dur-slow` («экран городов»),
  хотя в CLAUDE.md общее правило 200–300ms. Если нужно короче — одна цифра
  в `city-grid.tsx`.
- Фоновые размытые пятна (Background Glow из DESIGN.md) на экране городов
  **не рисовал** — в задаче их нет. Скриншоты — чистый Cream.
- Старый тест `e2e/home.spec.ts` (искал слово APETIT на временной главной)
  удалён, его заменили новые тесты.
- Lighthouse не гонял — по CLAUDE.md он нужен перед релизом, а не на каждом
  шаге.

### Что нужно от Амяна

1. **Перезапустить окно Claude Code** (закрыть и открыть заново), чтобы
   появился Context7 из `.mcp.json`. При первом запуске Claude Code спросит,
   доверять ли серверу из `.mcp.json` — ответить «да».
2. Запустить `npm run dev`, открыть http://localhost:3000, нажать город,
   потом снова открыть http://localhost:3000 — должно сразу показать
   выбранный город. Чтобы вернуться к списку: в браузере F12 → Application →
   Local Storage → удалить `apetit.city` (кнопка «сменить город» появится
   позже).
3. Передать архитектору вопросы ниже.

### Вопросы архитектору

1. Прислать обновлённый раздел «City Screen» DESIGN.md (см. выше) — или
   подтвердить, что действуют цифры из задачи, и тогда я перепишу раздел
   «City Tile» и токен `--radius-tile` сам.
2. Рисовать ли на экране городов фоновые пятна (Yellow .30 / Peach .38) из
   раздела Background Glow?
3. На десктопе FLOREȘTI при 44px занимает почти всю плитку 200px (запас
   ~15px с каждой стороны). Оставляем 200 или берём, например, 220?
4. На `/` по ТЗ нет ни одного слова, поэтому скринридер не знает, что это за
   страница. Можно добавить невидимый заголовок (только для скринридеров),
   но нужен текст: `[ТЕКСТ: заголовок экрана городов, напр. «Alege orașul» /
   «Выберите город»]`. Делаем?
5. Страница 404 сейчас стандартная от Next (белая, по-английски). Своя
   потребует текст: `[ТЕКСТ: страница не найдена + кнопка на главную]`.
   Когда делаем?

---

## 2026-09-19 — Этап 5 — DESIGN 2.1 в код + подготовка фото блюд

### Проверка DESIGN.md
В 5-й строке — «Версия: 2.1». Закоммичен как есть (`ff4c23d docs: design system
v2.1`) вместе с двумя макетами `docs/mock-*.png`, на которые он ссылается.
Позже с вашей стороны в DESIGN.md изменился раздел Background (источник фона —
кадр из видео `linii-fundal.mp4`); эту правку закоммитил отдельно
(`40d17b8`), чтобы она не потерялась.

### Ответы архитектора — что сделано

| № | Ответ | Что сделано |
|---|---|---|
| 1 | Код к DESIGN 2.1 | `globals.css` переписан: Cream `#FAF7F2`, Milk `#FFFDFA`; радиусов ровно четыре (`pill 9999 / input 12 / tile 16 / sheet 24`); линии `--rule`, `--rule-dotted`, `--hairline`; тени — только `lift` + «лужица» `--puddle`; токены Background Glow удалены; стекло по 2.1 (`.78`, blur 16 / saturate 130); шкала текста — добавлен `billboard`, `chip` 13px, `price-tile` 15px, `caption` заглавными +0.08em, `category` убран; размеры компонентов (`--size-chip 32`, `--size-price-pill 28/32`, кнопки, шапка, корзина…). `theme-color` — `#FAF7F2`. Плитка города — через `--rule` и `--radius-tile`. Тест `globals.test.ts` теперь сторожит именно правила 2.1 (9 проверок). |
| 2 | Пятен нет | Ничего не рисовал; токены удалены. |
| 3 | Десктоп 3 колонки | Сетка `grid-cols-3` по 1fr в контейнере 1200: ряд 3 + ряд 2, плитки 200px высотой (e2e-тест на 1280px проверяет ряды, ширины и высоту). Скриншот `docs/screens/01-orase-1280.png` обновлён. |
| 4 | Скрытый заголовок | Появилась **система переводов** `src/i18n/messages.ts` (ro/ru, один набор ключей — есть тест). На `/` — `<h1 class="sr-only">Alege orașul</h1>` (ru: «Выберите город»). e2e проверяет, что заголовок есть для скринридера, но невидим (1×1px), и что другого текста на экране нет. |
| 5 | 404 позже | Оставлена стандартная. |
| — | Скилл `grilling` | Установлен (`npx skills add mattpocock/skills --skill grilling -a claude-code`). Теперь `grill-me` может его вызвать. |

Коммит `c3469d4 refactor: align tokens and city screen with design system v2.1`.

**Важная поправка к нему:** этот коммит не проходил проверку типов при
`next build` (`error TS2322` в `messages.ts` — `as const` делал русский текст
«не того типа»). Dev-сервер и тесты типы не проверяют, а моя проверка сборки
искала слово `Error` с большой буквы и пропустила `error TS…`. Нашёл сам через
20 минут, исправил (`955220a fix: type i18n messages explicitly`), сборка
проверена по коду выхода. Впредь проверяю `npm run build` по коду выхода, а не
по grep.

### Подготовка фото — что сделано

Процесс: план (`docs/superpowers/plans/2026-09-19-product-images.md`) → тесты →
код → проверка.

1. **Установлено** (бесплатно, локально): `rembg[cli]` 2.0.84, `onnxruntime`
   1.30.0, `pillow` 12.3.0 — на наш Python 3.14 встали без проблем. Список —
   в `scripts/requirements.txt`. Модели скачались сами в `~/.rembg/models`.
2. **Скрипт `scripts/prepare-images.py`** (+ `prepare_images_lib.py` с чистыми
   функциями):
   - PNG с настоящей прозрачностью (21 файл: пиццы, напитки, хот-доги, меню,
     два соуса) → только обрезка пустых полей;
   - JPG на белом (29 файлов) → rembg, модель `isnet-general-use`, режим
     alpha matting. Маска считается на копии 2048px по длинной стороне (сама
     модель всё равно работает на 1024px, а матирование на 12 Мп заняло бы
     минуты на файл) и поднимается на исходник — мастер остаётся в исходном
     размере;
   - **автоматический запасной вариант:** если у вырезки находятся дефекты
     (дырки внутри фигуры, полупрозрачная белая посуда, слишком мало
     непрозрачных пикселей), файл пересчитывается второй моделью
     `birefnet-general-lite`, и остаётся результат с меньшим числом дефектов.
     Обе модели — MIT;
   - выход: `public/img/products/<slug>-400/-800/-1600.webp` (quality 82, без
     апскейла) + мастер `<slug>.png` в исходном размере (в `.gitignore`);
   - манифест `src/data/images.json`: slug → `width`, `height`, `sizes`,
     `method` (alpha/rembg), `model`, `flags`, `source`, `sourceHash`;
   - идемпотентность: повторный запуск сверяет sha1 исходника и наличие файлов
     и ничего не пересчитывает (2–3 секунды). `--force` — пересчитать всё,
     `--only slug…` — выборочно, `--sheet-only` — только лист;
   - `desktop.ini` пропущен. Файла `le-coq-margarita-mojito.png` в папке нет —
     есть `le-coq-margarita.png` и `le-coq-mojito.png` (в SPEC обе позиции есть
     в Drinks), обработал оба. Если их всё-таки не надо — скажите.
3. **Результат:** 50 позиций, 138 WebP (17 МБ в git), полный прогон ~11 мин.
   26 файлов вырезала isnet, 3 — birefnet (запасной вариант), 21 — уже были
   прозрачными.
4. **Контрольный лист** `docs/screens/02-produse-sheet.png` — все 50 на
   `#FAF7F2`, по 6 в ряд, подписи slug (красным — если остались флаги; сейчас
   таких нет). Команда: `npm run images`.
5. **Тесты:** Python `unittest` — 14 тестов на чистые функции
   (`npm run test:py`); Vitest `images.test.ts` — манифест валиден, все файлы на
   месте, нет сирот, **повторный запуск скрипта не меняет ни байта** (файлы,
   манифест и лист сравниваются по хэшам до и после). Всего `npm test` — 40
   тестов; e2e — 9; lint, build (по коду выхода) — чисто.

### Какие файлы вырезались плохо

Проверял двумя способами: эвристики в скрипте + все 50 глазами, проблемные —
крупно, вместе с картой альфа-канала.

| Файл | Что было (isnet) | Что сделано | Что сейчас |
|---|---|---|---|
| `supa-bostan.jpg` | два белых кубика брынзы — сквозные дырки, остальные полупрозрачные | пересчитан birefnet | **хорошо**, брынза сплошная |
| `sos-mustar-miere.jpg` | белая пиала на 18 % полупрозрачная | пересчитан birefnet | **хорошо** |
| `brinzoaice.jpg` | картонная коробочка полупрозрачная, левый край исчез | пересчитан birefnet — он **убрал коробочку почти целиком**, остались бледные фрагменты | **спорно**: пирожки чистые, но подача «в коробочке» потеряна. См. вопрос 1 |

Всё остальное — чисто: лаваш у кебабов и гёзлеме цел, белые пиалы соусов с
ободками, супы, багеты, бургеры, картошка. На кремовом фоне полупрозрачная
белая посуда была почти незаметна, но под будущим слоем линий фона она бы
«просвечивала» — поэтому и пересчитал.

### Что не встало / отличия от задания

- **Мастер-PNG в исходном размере, но маска считается на 2048px** (см.
  выше). На выходе до 1600px это не влияет.
- Для 7 узких картинок (бутылки) размеров меньше трёх — без апскейла: `cola`,
  `fanta`, `sprite`, `apa-gazata`, `le-coq-mojito` — только 400; `apa-plata`,
  `le-coq-margarita` — 400 и 800. Ширина вырезки 650–1171px. В манифесте
  `sizes` показывает, что есть.
- В `~/.rembg/models` кроме двух рабочих моделей (171 + 214 МБ) лежит
  `birefnet-general` (928 МБ) — скачал для сравнения, в итоге не используется.
  Папку можно удалить (`C:\Users\T3IS\.rembg\models\birefnet-general`) — сам
  не удаляю без подтверждения.
- Появилась папка `assets/brand/` с видео 200 МБ — добавил в `.gitignore`
  (как и остальные исходники в `assets/`), в git она не попала.
- Зависимости Python поставлены в системный Python (без venv) — так проще
  для одного человека на одном компьютере; список в `requirements.txt`.
- Два предупреждения `PERFORMANCE WARNING … Cholesky` при матировании — это
  внутренний fallback pymatting, на результат не влияет.

### Что нужно от Амяна

1. **Открыть `docs/screens/02-produse-sheet.png`** и проверить вырезки глазами:
   ничего ли не отъедено, нет ли лишних кусков. Особенно `brinzoaice`.
2. Перезапускать VS Code / Claude Code не нужно. Чтобы пересобрать фото после
   добавления новых файлов в `assets/foto-originale/` — `npm run images`
   (только новые, ~30 с на файл).
3. Передать архитектору вопросы ниже.

### Вопросы архитектору

1. `brinzoaice`: оставить вариант без коробочки (чисто, но пирожки «летят»)
   или нужна вырезка с коробочкой? Второе — только руками (Photoshop) или новое
   фото на контрастном фоне.
2. Узкие бутылки: 400px по ширине для плитки 150px высотой хватает с запасом,
   но если бутылки будут крупно (витрина города), нужны оригиналы шире или
   съёмка ближе.
3. Le Coq: обрабатывать оба (`le-coq-margarita`, `le-coq-mojito`) — верно?

---

## 2026-09-19 — Этап 5 — данные меню и страница меню

### Ответы архитектора
1. `brinzoaice` — оставлен без коробочки; 2. бутылки — 400px; 3. Le Coq — оба.
Заведён постоянный список **«Фото на пересъёмку»** — в начале этого файла.

### Что сделано

Процесс: план `docs/superpowers/plans/2026-09-19-menu-page.md` → «допрос»
(13 вопросов с принятыми ответами — в плане) → тесты → код → проверка по
`web-design-guidelines`.

**1. Данные меню — `src/data/menu/`**
- `schema.ts` — zod-схемы и типы: Category, Product, Variant, Addon,
  Removable, PointProduct.
- `categories.ts` — 12 категорий в порядке задачи + 13-я «Supe» в конце
  (супы выключены везде, поэтому категория нигде не показывается).
- `products.ts` — **57 блюд** из приложения А: название RO, состав RO,
  граммы, базовая цена, slug = имя файла фото. Варианты размера — у
  `kebab-xl-xxl` (80/99, 340/430 g), `cartofi-pai` (23/30, 120/180 g),
  `kebab-menu` и `burger-menu` (130/170, со своим составом у XL и XXL).
  Названия и составы на RU — черновые, помечены `TODO ru: проверить`.
- `addons.ts` — 18 добавок из SPEC 2.4.
- `point-products.ts` — доступность: пицца выключена в обеих точках Сорок,
  супы — везде, остальное включено везде по базовой цене.
- Без фото (`photo: null`, на сайте — песочная плитка с рисованной тарелкой):
  лимонады, чай, 4 вида кофе.
- Тесты (24): схемы принимают все данные; порядок категорий; slug уникальны;
  цены > 0 у блюд, вариантов, добавок; каждое фото есть в `images.json` или
  блюдо помечено «без фото»; варианты ровно у четырёх блюд; доступность по
  точкам; Сороки — 11 категорий без пиццы, Бричаны — 12.

**2. Страница `/[city]`** — по DESIGN.md 2.1 и макету (вариант A):
- **Шапка**: стекло, линия снизу, APETIT текстом Oswald, город + стрелка
  (нажатие = сменить город, уже работает: возврат на экран городов), RO / RU.
- **Лента чипов**: 32px, рисованная иконка 16px + название; активный — Ink.
  Чипы — якоря; при скролле активный чип меняется сам (IntersectionObserver)
  и лента докручивается, чтобы он был виден.
- **Рисованные иконки** — 13 штук в `src/components/icons/` (12 категорий +
  тарелка для заглушки): одна линия 1.6px, слегка неровные. **Ждут
  утверждения Амяна** — посмотреть можно на скриншотах в чипах.
- **Категория**: точечная линия → слово-вывеска контуром → сетка плиток
  2 колонки (телефон) / 4 (десктоп), первая строка наезжает на слово.
- **Плитка блюда**: фото WebP через next/image (srcset из готовых 400/800/1600,
  «лужица» под фото), название 2 строки, состав 2 строки с «…», граммы,
  ценник («de la 80 lei» при вариантах), кнопка «+» (пока без действия).
- Otaci открывается на русском (названия, чипы, «от 80 лей»).
- Анимации: плитки появляются один раз при скролле (y 12→0, задержка 40ms
  по колонкам), нажатие чипа — плавный скролл. При «уменьшить движение» —
  без анимации.

**3. Тесты**: Vitest — 72 (было 40); Playwright на 390px — 17: /soroca —
11 категорий без пиццы; /briceni — есть пицца; клик по «Burgers» скроллит к
BURGERS и делает чип активным; все фото загружаются (47 картинок, ни одной
битой, ни одного 404); Otaci на русском; слова-вывески в одну строку и во
всю ширину (RO и RU); на десктопе лента чипов на одной линии с контентом и
4 колонки. Lint ✓, build ✓ (по коду выхода).

**4. Скриншоты**: `docs/screens/03-meniu-390.png`, `03-meniu-1280.png`.

### Решения, которые я принял сам (прошу подтвердить)

1. **Размер слова-вывески.** Токен DESIGN.md — `clamp(72px, 22vw, 180px)`, это
   86px на телефоне: KEBAB занимал 63 % ширины, а в утверждённом макете и в
   тексте DESIGN («во всю ширину контента») — почти всю. Хуже того, SANDWICH
   при 86px не помещался и **рвался посреди буквы** на две строки. Сделал
   подгонку: слово ложится ровно во всю ширину, потолок 180px из токена
   сохранён (на десктопе слово 180px). Ширина букв Oswald замерена и лежит
   таблицей в `src/lib/billboard-fit.ts`. Если нужно строго 22vw — одна
   строка в `globals.css`, но тогда длинные слова придётся переносить.
2. **Состав кебабов и бургеров.** В SPEC только общая база («все кебабы
   содержат…»). Добавил отличительный ингредиент из названия, как в макете:
   Kebab Cheese → cașcaval, Crispy → crispy, Philly Beef → vită + cașcaval,
   чизбургеры → cașcaval + pui / vită-porc / crispy.
3. **Цены соусов отдельно** — по печатному меню (ketchup 10, maioneză 15), а
   не по кассе (8, 10). SPEC: УТОЧНИТЬ.
4. **Sos Apetit** — одно блюдо с вариантами dulce / picant по 15 (фото одно).
5. **Apă plată / gazată** — два блюда по 18 (у каждой своё фото).
6. **Названия**: «Sandwich cu salam fiert-afumat», «Sandwich cu șuncă de
   găină», «Salată Grecească», «Salată Cezar»; пиццы — без слова «Pizza»
   (под вывеской PIZZA).
7. **Что можно убрать** (для листа блюда) — весь состав, кроме основы
   (lipie, chiflă, tortilla, ciabatta).
8. **Добавки по категориям** — по предположению из SPEC 2.4: ингредиенты — к
   kebab, burgers, gözleme, hot dog, sandwich; соусники — ещё к menu, crispy,
   salad, pizza; к соусам, напиткам и десерту — ничего.

### Какие тексты нужны от Амяна

- **Русские названия и составы всех 57 блюд, 12 категорий и 18 добавок** —
  сейчас черновой перевод (`TODO ru`). Удобнее всего — проверить список
  в `src/data/menu/products.ts` или я выгружу таблицей.
- Подпись ленты категорий для скринридера: сейчас «Categorii» / «Категории» —
  это моё слово, подтвердите или замените.
- Слова «Schimbă orașul», «Adaugă», «de la», «lei» взяты из DESIGN.md,
  шаблон заголовка вкладки «Apetit Soroca — kebab, burgeri, comandă online» —
  из SPEC §8; RU-версии — черновик.

### Что не встало / отличия

- **Скилл `grilling` (grill-me) интерактивный** — он ждёт ответов человека.
  Вопросы «допроса» и мои ответы записаны в плане; всё, что требует решения
  архитектора, вынесено выше и в вопросы.
- **Переключатель RO / RU пока не работает** — только показывает язык.
  По SPEC §7 русская версия живёт на `/ru/soroca`, такого маршрута ещё нет.
  Для Otaci русский включается сам, но у страницы `<html lang="ro">`, а
  русский язык стоит на обёртке страницы.
- Lighthouse не гонял (по CLAUDE.md — перед релизом).
- Между точечной линией и словом-вывеской 48px воздуха, как в DESIGN.md; в
  макете этот зазор визуально меньше.

### Что нужно от Амяна

1. Посмотреть `docs/screens/03-meniu-390.png` и `03-meniu-1280.png` — или
   `npm run dev` и открыть http://localhost:3000/soroca, полистать меню,
   понажимать чипы.
2. Утвердить рисованные иконки в чипах (или сказать, какие перерисовать).
3. Русские тексты (см. выше).

### Вопросы архитектору

1. Слово-вывеска: подгонка «во всю ширину» (сейчас) или строго 22vw?
2. Состав кебабов/бургеров, цены соусов, Sos Apetit, названия сэндвичей и
   салатов, добавки по категориям — решения 2–8 выше верны?
3. Супы: продаются ли? Сейчас выключены везде. Напитки: во всех ли точках?
4. Кофе и чай тоже без фото — добавить их в «Фото на пересъёмку»?
5. Маршрут `/ru/[city]` и рабочий переключатель RO/RU — отдельной задачей?

---

## 2026-09-19 — Этап 5 — лист блюда и корзина

### Ответы архитектора — что сделано

| № | Ответ | Что сделано |
|---|---|---|
| 1 | Слово-вывеска во всю ширину, потолок 180px | DESIGN.md 2.2 закоммичен (`6e29744 docs: design system v2.2`). Код уже так работал. |
| 2 | Решения 2–8 подтверждены | В коде ничего не менял. Одно следствие правила 7 для комбо — вопрос 1 ниже. |
| 3 | Супы не продаются, напитки — везде | Так и было: супы выключены, данные на месте; напитки включены во всех точках. |
| 4 | Кофе и чай — в «Фото на пересъёмку» | Добавлены в список в начале файла. |
| 5 | `/ru/` — отдельной задачей | Не трогал. |

### Что сделано

Процесс: план `docs/superpowers/plans/2026-09-19-product-sheet-and-cart.md` →
«допрос» (24 вопроса с принятыми ответами — в плане) → тесты → код → проверка
по `web-design-guidelines` и `review-animations`.

**1. Лист блюда.** Нажатие на фото или название плитки открывает лист: на
телефоне выезжает снизу, на десктопе — окно 520px по центру. Внутри: фото
280px с «лужицей», название Oswald 22px заглавными, состав, граммы; блок
**MĂRIME** — кнопки-сегменты (выбранный жёлтый), только у блюд с размерами;
**FĂRĂ … gratuit** — галочки, отмеченный ингредиент зачёркивается; **EXTRA** —
добавки, разрешённые категории, цена справа «+15 lei». Внизу всегда видны
«− N +» и жёлтая кнопка «Adaugă · 228 lei» — цена меняется сразу. Размер
меняет и граммы, а у комбо — и состав. Закрыть: свайп вниз, крестик, тап по
затемнённому фону, Esc. Фокус с клавиатуры не уходит под лист, после закрытия
возвращается на плитку. Сделан на встроенном в браузер элементе `<dialog>` —
он сам делает остальную страницу недоступной, это надёжнее самописного.

**2. Корзина — Zustand** (новая библиотека, `zustand` 5.0.15, MIT, бесплатная,
≈ 2 КБ в сжатом виде вместе с сохранением). Почему не React context:
- при context каждое нажатие «+» перерисовывает все ~50 плиток и бар; в
  Zustand плитка подписана только на своё число и перерисовывается одна —
  важно для дешёвых Android;
- готовое сохранение в localStorage с версией (на будущее — миграция формата);
- корзина доступна без React — её логика проверяется обычными быстрыми тестами.

Позиция = блюдо + размер + добавки + убранные ингредиенты + количество, только
id, **никаких цен и названий** — сервер посчитает всё сам (SPEC 9.3).
Одинаковые конфигурации складываются (порядок галочек не важен). Функция
расчёта цены (`src/lib/cart/pricing.ts`) проверяет всё по id: размер
существует, добавка разрешена этому блюду, убираемый ингредиент есть в составе,
количество 1–99. Эту же функцию возьмёт сервер при приёме заказа. Корзина
лежит в localStorage (`apetit.cart`) вместе с городом; мусор там не ломает
страницу; открытая вторая вкладка видит изменения первой.

**3. Кнопка «+» на плитке.** Блюдо без размеров: «+» кладёт 1 шт. как есть,
без листа, и превращается в чёрную пилюлю «− N +» (N — все штуки этого блюда в
корзине). Блюдо с размерами: «+» открывает лист, после добавления — чёрный
кружок с числом. Нажатие — сжатие до .92; значок корзины внизу один раз
подпрыгивает.

**4. Cart Bar и лист корзины.** Стеклянная полоса снизу с линией сверху
выезжает, когда в корзине есть хоть одна позиция: корзина с бейджем (штуки),
«1 poziție», жёлтая «Coș · 228 lei». Нажатие открывает лист корзины: позиции с
фото, размером, «Extra: …», «Fără: …», «− N +», «Șterge», итог; «Comandă»
пока серая (оформление — следующая задача). Удалили последнюю позицию — лист
закрывается, бар уезжает. **На десктопе** бара нет — по DESIGN.md → Layout
корзина — кнопка в шапке.

**5. Смена города.** Если в корзине позиции Сорок, а человек открыл Бричаны
(кнопкой в шапке, ссылкой из Google, закладкой — неважно), появляется
подтверждение «Ai schimbat orașul — coșul va fi golit»: «Continuă» — корзина
очищается, «Anulează» (и Esc) — возврат в Сороки, корзина цела. Пока решение не
принято, корзину Сорок в Бричанах не показываем.

**6. Тексты** — в `messages.ts` RO/RU (список ниже).

**7. Проверки.** Vitest — **132** теста (было 72): сложение одинаковых
конфигураций, пересчёт цены (размер + добавки × количество = 228), очистка при
смене города, отказ добавлять в корзину другого города, мусор в хранилище, две
вкладки, склонение «poziție / позиций», формат цены. Playwright на 390px —
**27** (было 17), новые 10: сценарий задачи целиком (Kebab XL/XXL → XXL → sos
usturoi → 2 шт. → «Adaugă · 228 lei» → бар «1 poziție», 228 lei → корзина →
удалить → бара нет); «+»/«−» на плитке и фокус; «+» у блюда с размерами;
«Fără»/«Extra» и сложение; закрытие Esc/крестиком/фоном и фокус внутри листа;
**свайп** (короткий — лист возвращается, длинный — закрывается); корзина после
перезагрузки; смена города (оба ответа); мусор в localStorage; десктоп (окно
520px, кнопка в шапке). Корзинные e2e прогнал 3 раза подряд — 30/30.
`npm run lint` ✓, `npm run build` ✓ (код выхода 0), весь e2e 27/27 ✓.

**8. Скриншоты:** `docs/screens/04-produs-390.png`, `04-cos-390.png`,
`04-produs-1280.png` (команда `npm run screens`).

### Что нашли и исправили по ходу (важное)

- **Лист сам закрывался в режиме разработки.** React при разработке нарочно
  монтирует всё дважды; браузер после первого закрытия присылал событие
  «закрыт» уже во второй, открытый лист. Нашли тестами, исправили.
- **Ранний «+» терялся.** Если нажать «+» в первые миллисекунды после загрузки,
  пока корзина ещё не подставилась из памяти, нажатие молча пропадало. Теперь
  корзина подставляется в момент нажатия — ничего не теряется и не затирается.
- **Комбо.** По правилу «убрать можно всё, кроме основы» у Kebab Menu в «Fără»
  оказались бы «kebab mic», «limonadă», «cartofi pai mic». Сделал: у комбо блока
  «Fără» нет (вопрос 1).
- По проверке `web-design-guidelines`: цена не разрывается переносом («228» и
  «lei» всегда вместе), суммы от 1000 — по правилам языка (рус. «1 250 лей»);
  наведение мышью на размеры и галочки; ровный перенос заголовков листов.
- По `review-animations`: прыжок корзины не перезапускается при частых «+»;
  кнопка корзины в шапке появляется не одной прозрачностью, а с лёгким
  увеличением.

### Что не встало / отличия от задания и DESIGN.md

- **Анимации — CSS, а не библиотека Motion.** Лист, фон, бар, счётчик —
  CSS-переходы; прыжок корзины — встроенная анимация браузера. Они не дёргаются,
  пока грузятся фото, и прерываются на полпути (скилл Эмиля). Длительности —
  токены DESIGN.md: появление листа 240ms, закрытие 150ms, нажатия 150ms; при
  «уменьшить движение» всё мгновенно.
- **Жёлтая кнопка в баре 48px, а не 52px**: бар 72px с отступами 12px — 52 не
  помещается. В шапке на десктопе — 40px (шапка 56px).
- **Ценника в листе блюда нет** (DESIGN: «в листе и корзине — крупнее»): цена
  уже на кнопке «Adaugă · 228 lei», а ценник за 1 шт. рядом с суммой за 2 шт.
  путал бы. В корзине — крупные ценники у каждой позиции.
- **Галочки — квадрат без скругления**: радиусов на сайте ровно четыре, а 12px
  на квадрате 22px — почти круг (похоже на радио-кнопку).
- **Свайп проверен в эмуляции Chrome.** Настоящий iPhone и дешёвый Android нужно
  проверить руками — жесты там ведут себя по-своему.
- **Полоска «домой» на iPhone**: отступ `env(safe-area-inset-bottom)` у бара и
  листа заложен, но заработает только с `viewport-fit=cover` в настройках
  страницы — это меняет раскладку всей страницы, без решения не трогал
  (вопрос 3).
- «Șterge» удаляет сразу, без «отменить»: позицию легко добавить снова, а
  всплывающие плашки запрещены.
- На узком телефоне (360px) счётчик «− N +» переносится под ценник — иначе не
  помещается.
- Скилл `grilling` (grill-me) интерактивный — вопросы и мои ответы в плане.
- Lighthouse не гонял (по CLAUDE.md — перед релизом).

### Тексты на утверждение Амяну

От архитектора (временные): Adaugă/Добавить · Mărime/Размер · Fără/Без ·
Extra/Добавки · gratuit/бесплатно · Coș/Корзина · poziție/poziții —
позиция/позиции · Total/Итого · Șterge/Удалить · Comandă/Заказать ·
«Ai schimbat orașul — coșul va fi golit» / «Вы сменили город — корзина будет
очищена» · Continuă/Продолжить · Anulează/Отмена.

Мои (грамматика и подписи для скринридера; на экране видны только первые две):
- «20 **de** poziții» (румынский, от 20) и «5 **позиций**» (русский, 5–20);
- «Închide» / «Закрыть» — крестик;
- «Scade cantitatea» / «Уменьшить количество», «Mărește cantitatea» /
  «Увеличить количество» — кнопки − и +;
- «Cantitate» / «Количество» — подпись счётчика.

### Что нужно от Амяна

1. Посмотреть скриншоты `docs/screens/04-*.png`.
2. Попробовать самому: в терминале `npm run dev`, открыть
   http://localhost:3000/soroca. Телефонный вид — F12, затем Ctrl+Shift+M.
   Понажимать «+», открыть блюдо, собрать корзину, удалить позицию. Смахивание
   листа вниз работает пальцем на телефоне (мышью — нет, это нормально).
3. Утвердить тексты выше.
4. Передать архитектору вопросы ниже.

### Вопросы архитектору

1. **Комбо без «Fără»** — верно? И по тому же правилу 7 сейчас можно бесплатно
   убрать главный ингредиент: «carne de pui» из Cheeseburger Pui, «crenvușcă»
   из хот-дога, «salam» из сэндвича с салями. Оставляем или основой считать и
   их?
2. **«Extra» у кебаба — 18 строк** (11 добавок в блюдо + 7 соусников) —
   длинный список. Разделить на два блока «Extra» и «Sosieră» (нужен текст
   заголовка) или оставить?
3. **iPhone:** включить `viewport-fit=cover` (бар и лист отступят от полоски
   «домой»; придётся проверить отступы всей страницы в горизонтальном
   положении)?
4. **Кнопка «Continuă»** в подтверждении — web-design-guidelines советуют
   конкретную подпись, например «Golește coșul» / «Очистить корзину». Меняем?
5. Ценник в листе блюда и квадратные галочки — как сделано, подходит?

## 2026-09-19 — Скиллы безопасности и правила их применения

### Что сделано
- Установлены в `.claude/skills/` (для Claude Code, в проект):
  - `agamm/claude-code-owasp`: **owasp-security**
  - `trailofbits/skills`: **semgrep**, **supply-chain-risk-auditor**,
    **differential-review**, **secure-workflow-guide**
  - `skills-lock.json` обновлён (фиксирует источник и версию каждого).
- **insecure-defaults** через `npx skills` не ставится: 5 августа 2026
  Trail of Bits переделали его из скилла в плагин Claude Code (команда +
  сценарий с несколькими агентами), папки со SKILL.md больше нет. Поставлен
  как плагин того же репозитория на уровне проекта:
  `claude plugin marketplace add trailofbits/skills --scope project` и
  `claude plugin install insecure-defaults@trailofbits --scope project`.
  Запись — в `.claude/settings.json` (закоммичен). Запуск:
  `/insecure-defaults:audit <файл или папка>`.
- Программы для semgrep:
  - `semgrep` 1.177.0 — через `pip install --user semgrep` (так рекомендует
    Semgrep), `semgrep --version` работает. Папка
    `%APPDATA%\Python\Python314\Scripts` добавлена в PATH пользователя.
  - `jq` 1.8.2 — через `winget` (бесплатная утилита для JSON, без неё
    скрипт скилла не запускается).
- В CLAUDE.md, раздел «Скиллы», добавлен блок «Безопасность» с четырьмя
  правилами из задания + как запускать insecure-defaults и одна оговорка
  про Windows (ниже).
- `.semgrepignore` — semgrep не сканирует `.claude/` (чужой код скиллов),
  `node_modules`, сборку и всё из `.gitignore`.
- В `.gitignore` добавлено `static_analysis_semgrep_*/` — туда скилл по
  умолчанию складывает отчёты, в git им не место.
- Тесты: `npm test` — 132 из 132, `npm run lint` — чисто.

### Semgrep — первый полный прогон
Бесплатная версия (Pro нет — анализ в пределах одного файла, связи между
файлами не отслеживаются). Режим «все находки». План утверждён Амяном.

| Набор правил | Файлов | Находок |
|---|---|---|
| p/security-audit | 107 | 1 |
| p/secrets (ключи, пароли в коде) | 107 | 0 |
| p/javascript, p/typescript, p/react, p/nodejs | 83 | 0 |
| p/nextjs | 0 | 0 — набор в реестре Semgrep сейчас пустой (`rules: []`) |
| p/python | 3 (ручной прогон) | 1 (та же, что в security-audit) |
| Trail of Bits | 107 | 0 |
| elttam | 107 | 0 (12 правил для Java не скомпилировались — к нам не относятся) |
| Apiiro (вредоносный код) | 86 | 2 (4 правила для Dart/Java/JS не скомпилировались) |

Итого 3 находки, **все три — ложные срабатывания, исправлять нечего**:
1. `src/lib/city-redirect-script.test.ts:12` — `new Function(...)`
   («динамическое выполнение кода»). Это тест: он исполняет наш же скрипт
   редиректа на город, чтобы проверить его поведение. В сайт не попадает,
   чужие данные туда не приходят.
2. `src/lib/city-redirect-script.test.ts:34` — «обфускация: глубокая
   вложенность анонимных функций». Это стрелочная функция внутри `expect` в
   том же тесте.
3. `scripts/prepare_images_lib.py:115` — SHA1. Используется как контрольная
   сумма файла фото (понять, изменилась ли картинка), не для защиты. Для
   этого SHA1 годится.

Оговорки по прогону:
- Скрипт скилла написан под Linux/Mac. На Windows `jq` добавляет `\r` в конец
  строк, и скрипт останавливается. Обход: временная обёртка, запускающая
  `jq -b`. Сам скилл не менял. Записано в CLAUDE.md.
- Набор `p/python` скрипт скилла на Windows запустил на 0 файлов (фильтр по
  расширению не сработал). Перезапущен вручную на `scripts/` — 3 файла,
  1 находка (SHA1, см. выше). Пробела в проверке нет.

### supply-chain-risk-auditor — зависимости
npm (`package.json` + `package-lock.json`), 18 прямых зависимостей:
- **Известных уязвимостей нет** ни в одной из 18 прямых и ни в одном из 475
  пакетов, которые они подтягивают (по версиям из lock-файла).
- Ни один пакет не запускает скрипты при установке, ни один не заброшен и не
  помечен устаревшим.
- Отмечено (не опасно, к сведению): `@types/node`, `@types/react`,
  `@types/react-dom` публикует один человек (это нормально для DefinitelyTyped,
  и это пакеты только для разработки); у `next`, `eslint-config-next`,
  `@playwright/test` в репозиториях лежат бинарные файлы (оценка OpenSSF
  Scorecard). Действий не требуется.
- **zustand 5.0.15**: уязвимостей нет, 40 млн скачиваний в неделю, активно
  развивается (обновлялся сегодня), публикуется из CI с подтверждением
  происхождения, скриптов установки нет. Нет файла политики безопасности —
  не риск. Вердикт: оставляем.
- **motion 13.4.0**: уязвимостей нет, 15,4 млн скачиваний в неделю, активен
  (обновлялся 2 дня назад), публиковать могут 2 человека, скриптов установки
  нет. Нет подтверждения происхождения сборки, нет политики безопасности,
  нет отчёта OpenSSF Scorecard — эти пункты «не проверить», а не «плохо».
  Вердикт: оставляем.

Python-скрипты фото (`scripts/requirements.txt`: rembg, onnxruntime, pillow,
numpy): уязвимостей нет. Зависимости второго уровня не проверены — для pip
нет lock-файла. Скрипты работают только на компьютере Амяна, в сайт не
попадают.

### Что не получилось / отличия от задания
- `insecure-defaults` — плагин вместо скилла (см. выше). Один прогон
  запускает ~8 агентов и больше — дороже, чем обычный скилл.
- `secure-workflow-guide` установлен, как просили, но он про смарт-контракты
  блокчейна (Slither, ERC-токены). Для сайта бесполезен, в правилах
  CLAUDE.md не упомянут.
- Отчёты semgrep и supply-chain лежат во временной папке, не в git.

### Что нужно от Амяна
1. Перезапустить VS Code, чтобы новое окно видело `semgrep` и `jq` без
   ручного добавления в PATH.
2. При следующем открытии проекта Claude Code может спросить, доверять ли
   маркетплейсу `trailofbits` и плагину `insecure-defaults` — ответить «да».

### Вопросы архитектору
1. `secure-workflow-guide` бесполезен для сайта (смарт-контракты). Удалить
   или оставить?
2. `insecure-defaults` теперь — сценарий из ~8+ агентов. Применять на каждую
   правку настроек, как в правиле, или только перед релизом вместе с semgrep?

## 2026-09-19 — Этап 5 — оформление заказа (+ ответы по безопасности и корзине)

### Ответы архитектора — что сделано

| № | Ответ | Что сделано |
|---|---|---|
| Б1 | `secure-workflow-guide` удалить | Удалён (`npx skills remove`), из `skills-lock.json` ушёл сам. В CLAUDE.md упоминаний не было. |
| Б2 | `insecure-defaults` — перед релизом и при крупных изменениях настроек | Правило в CLAUDE.md переписано по ответу. |
| К1 | База = хлеб + главный ингредиент из названия | У каждого блюда поле `main` (что в названии). Базу нельзя убрать, и её нет в «Fără». Опечатка в `main` роняет сборку данных — тихо не пропустит. Спорные блюда — вопрос 1 ниже. |
| К2 | «Extra» / «Sos aparte» | Два блока в листе блюда и в описании позиции (корзина, оформление, подтверждение). Раздел по SPEC 2.4 однозначный: «в блюдо» (8 ингредиентов + 3 соуса внутрь) → Extra, 7 «Sosieră» → Sos aparte. Непонятных нет. |
| К3 | `viewport-fit=cover` + safe-area | Включено. Шапка уходит под «чёлку» стеклом, текст — ниже неё; лента категорий и якоря сдвинуты на ту же высоту; бар корзины и лист — отступ от полоски «домой» и от боковых вырезов в горизонтальном положении. |
| К4 | «Golește coșul» с подтверждением вторым нажатием | Уточнил у Амяна: новая кнопка **в корзине** (окно смены города не трогал). Secondary-кнопка под списком; первое нажатие — чёрная «Da, golește», второе — очищает; через 4 с или если ушёл фокус — возвращается. |
| К5 | Ценник и квадратные галочки — как есть | Не трогал. |

### Что сделано — оформление заказа

Процесс: план `docs/superpowers/plans/2026-09-19-checkout.md` (решения там же) →
тесты → код → проверки `owasp-security`, `differential-review`,
`web-design-guidelines`; анимация — по скиллу `animate` (Эмиль Ковальски);
форма — по правилам `ui-ux-pro-max`; Next 16 — по `node_modules/next/dist/docs`
и context7.

**Страница `/[city]/comanda`** (язык — города: Otaci по-русски). Открывается
кнопкой «Comandă» в корзине (раньше была серой). Пустая корзина → в меню.
Сверху вниз, как в задаче: «Înapoi la meniu» → [баннер «закрыто»] → COMANDĂ →
**Punctul** (только Сороки: две карточки, выбор обязателен) → **Nume**,
**Telefon** (маска `069 123 456` / `+373 69 123 456`, клавиатура с цифрами),
**Adresă** («dacă vrei livrare») → ловушка для ботов (невидима) →
«Trimite comanda» + «Casierul te va suna pentru confirmare» → список позиций и
Total. На десктопе список справа.
- Ошибки — под полем, при уходе с поля; при отправке фокус на первое неверное
  поле. Исправил — ошибка исчезает сразу.
- Нет сети / сервер не ответил — строка под кнопкой, всё введённое на месте,
  можно нажать ещё раз.
- Вне 08:30–23:00 (время Кишинёва) — баннер «Primim comenzi 08:30–23:00» здесь
  и в корзине, кнопки неактивны. Часы телефона — только для показа; если
  телефон «врёт», решает сервер (есть тест).

**Сервер** — Server Action (`src/app/[city]/comanda/actions.ts`) → модуль
`src/server/orders/` → `submitOrder()`. Почему не route handler: Next 16 сам
защищает Server Action от подделки запроса с чужого сайта (CSRF), а отдельный
публичный адрес API не нужен. Проверки по порядку: всё через zod (лишние поля,
например сумма, — отказ) → ловушка → точка есть и не на паузе («временно не
принимает» — новое поле точки) → часы → каждая позиция продаётся в точке и
собрана из разрешённого → повтор за 2 минуты → лимиты → номер → снимок заказа.
**Цену считает только сервер** по каталогу выбранной точки. Сейчас заказ
пишется в лог сервера (без полного телефона, без имени и адреса) и
возвращается номер. Telegram и Supabase следующая задача добавит в эту же
функцию, форма не изменится.
- **Номер** — по SPEC 9.3: сквозной с 1001.
- **Лимиты** (SPEC 9.4): ≤3 заказа с номера за 10 минут (номер в любом виде —
  `067…` и `+373 67…` — один); тот же заказ с того же номера за 2 минуты —
  возвращается тот же номер, кассир получит один заказ; плюс лимит по IP —
  10 заказов за 10 минут (в SPEC 9.4 есть «лимит по IP»), работает на
  Cloudflare.
- **Всё это пока в памяти сервера** — `// TODO: перенести в Supabase`. После
  перезапуска номера снова с 1001; на Cloudflare у каждого экземпляра своя
  память — лимиты и номера не общие. До запуска обязательно в базу.

**Подтверждение `/[city]/comanda/1001`** (адрес по SPEC §8): жёлтый круг с
галочкой, «NR. 1001», «Te sunăm în câteva minute», точка, состав, Total,
«Sună la local · 068 372 707» (звонок), «Înapoi la meniu». Появляется
спокойно: 240ms, лесенкой по 60ms; при «уменьшить движение» — сразу.
Корзина очищается только после ответа сервера. Базы нет — данные экрана
хранятся во вкладке; открыть этот адрес в другой вкладке или на другом
телефоне нельзя (уводит в меню).

**Проверки.** Vitest — **187** (было 132): часы (лето/зима, 08:29/08:30,
22:59/23:00), телефон (8 верных, 10 неверных форм, маска), схема (длины,
буквы, невидимые символы, сумма от клиента), `submitOrder` — 15 (цена 228,
номера, лог без телефона, вне часов, ловушка, пауза, пицца в Сороках, чужой
размер/добавка/база, дубль, лимит номера, лимит IP), снимок, база блюд,
очистка корзины. Playwright — **37** (было 27): путь целиком (город → блюдо →
корзина → оформление → подтверждение → корзина пуста), пустая корзина, плохой
телефон и не выбран пункт, вне часов (Otaci по-русски + корзина), «часы
телефона врут», пицца в Сороках, ловушка, обрыв сети и повтор, «Sos aparte»,
«Golește coșul». `npm run lint` ✓, `npm run build` ✓ (код выхода 0).
Один раз из ~15 прогонов unit-тестов один тест упал — сразу после
автоформатирования файлов; повторить не удалось (13 прогонов подряд — все
зелёные). Слежу.

**Скриншоты:** `docs/screens/05-comanda-390.png`, `05-comanda-inchis-390.png`,
`05-confirmare-390.png`, `05-comanda-1280.png`; переснял `04-*` (в корзине
новая кнопка, в листе — «Sos aparte»).

### Безопасность (owasp-security + differential-review)

Отчёт: `docs/security/2026-09-19-checkout-review.md`. Коротко:
- Исправлено сразу: телефон точки из сохранённого во вкладке снимка теперь
  проверяется по формату (он идёт в ссылку `tel:`).
- Известное и отложено по задаче: лимиты и номера в памяти → Supabase.
- IP для лимита берётся только из заголовка Cloudflare. На Vercel (план Б) его
  можно подделать — при переезде поменять.
- Для следующей задачи записано в коде: имя и адрес экранировать в сообщении
  Telegram; заказ из базы не отдавать просто по номеру — номера легко
  перебрать.
- Заголовков безопасности (CSP, HSTS) ещё нет — перед релизом
  (`insecure-defaults`).
- Тестовое время для e2e работает только в тестовом сервере, в боевой сборке
  выключено.

### Что не получилось / отличия

- **Расстояние «~1,2 km» не видно**: в SPEC нет ни адресов, ни координат
  точек. Расчёт написан и проверен тестом; разрешение на геолокацию сайт
  спросит, только когда у точек появятся координаты (сейчас не спрашивает).
  В карточках точек вместо адреса — `[ТЕКСТ: адрес точки]`.
- Поля ввода — шрифт 16px, а не 15px из DESIGN: иначе iPhone при нажатии на
  поле увеличивает всю страницу.
- У выбранной карточки точки, кроме жёлтой рамки 2px (DESIGN), — чёрная
  галочка: жёлтая рамка на кремовом фоне почти не видна.
- Во время отправки кнопка просто неактивна — без «Se trimite…» (нужен текст)
  и без крутилки (запрещено).
- Нашёл и исправил свою ошибку по ходу: одна из правок стилей обрезала конец
  `globals.css`. Восстановил из git; разница с прошлой версией — только
  нужные правки, все тесты зелёные.
- Semgrep не гонял — по правилу он перед релизом.

### Решения, которые я принял сам (прошу подтвердить)

1. **База блюд.** Сыр в названии = база: у Kebab Cheese, всех Cheeseburger,
   Hot Dog Cheese сыр убрать нельзя. Philly Beef — база только vită (сыр
   убирается). Quattro Formaggi — 4 сыра (с грушей — и груша), 4 Cărnuri —
   4 мяса, Pepperoni — салями, Șuncă și Legume — ветчина. У салатов,
   Margarita, Carbonara и Kebab XL/XXL в названии ингредиента нет — убрать
   можно всё, кроме хлеба. Crispy (Aripioare и др.) — в составе только соус,
   его убрать можно.
2. В Сороках точка **не выбрана заранее** — человек выбирает сам (пока нет
   расстояний, «ближайшую» подставить нечем).
3. Сработала ловушка для ботов → «заказ не принят» (без подробностей).

### Тексты на утверждение Амяну

Из задания (временные): Punctul · dacă vrei livrare / если нужна доставка ·
Trimite comanda / Отправить заказ · Casierul te va suna pentru confirmare /
Кассир перезвонит для подтверждения · Primim comenzi 08:30–23:00 / Принимаем
заказы 08:30–23:00 · Te sunăm în câteva minute / Перезвоним через несколько
минут · Înapoi la meniu / Вернуться в меню · Golește coșul / Очистить
корзину · Da, golește / Да, очистить · Sos aparte / Соус отдельно.

Мои (на утверждение): Comandă / Заказ (заголовок) · Nume, Telefon, Adresă /
Имя, Телефон, Адрес · «0XX XXX XXX» (подсказка в поле телефона) · Nr. 1001 /
№ 1001 · Sună la local / Позвонить в заведение (слово из DESIGN, ru —
перевод) · Пункт (ru для «Punctul») · km / км.

Нужны от Амяна (сейчас на экране `[ТЕКСТ: …]`, ro и ru):
1. адрес каждой точки (6 шт.);
2. ошибка: не выбран пункт;
3. ошибка имени (2–40 букв);
4. ошибка телефона (формат 0XX XXX XXX);
5. ошибка адреса (до 120 символов);
6. не отправилось — проверьте интернет и нажмите ещё раз;
7. части блюд нет в этом пункте — уберите их;
8. пометка у такой позиции: «нет в этом пункте»;
9. пункт временно не принимает заказы;
10. слишком много заказов с этого номера — позвоните;
11. заказ не принят — попробуйте ещё раз.

### Что нужно от Амяна

1. Посмотреть скриншоты `docs/screens/05-*.png`.
2. Попробовать: `npm run dev`, http://localhost:3000/soroca → добавить блюдо →
   корзина → «Comandă» → заполнить → «Trimite comanda». Номер заказа появится
   и в окне терминала (строка `[orders] order accepted`).
3. **Адреса и координаты точек** — для каждой из 6 точек ссылку на Google
   Maps (из неё возьму координаты для «~1,2 km»).
4. Тексты выше.

### Вопросы архитектору

1. База блюд (решение 1 выше) — так?
2. **Итог внизу.** По задаче порядок: поля → кнопка → список и сумма. На
   телефоне сумму видно, только если прокрутить ниже кнопки. Переставить итог
   над кнопкой?
3. В SPEC шаг 5 есть кнопка «Определить, где я» у адреса, а в шаге 6 — «имя,
   телефон, адрес запоминаются на устройстве». В задаче их не было — не
   делал. Отдельной задачей?
4. Номер заказа «NR. 1001» / «№ 1001» — такой вид подходит?
5. Supabase для номеров и лимитов — следующей задачей вместе с Telegram?

## 2026-09-19 — Этап 5 — доработки оформления, тексты, точки

### Ответы архитектора — что сделано

| № | Ответ | Что сделано |
|---|---|---|
| 1 | База блюд — верно | Код не менял. |
| 2 | Телефон: поля → «Coș» с Total → кнопка → строка про звонок; десктоп — колонка справа | Сделано. На телефоне сумма видна до кнопки; на десктопе «Coș» справа, сверху на уровне формы. Тесты проверяют порядок на обоих экранах. |
| 3 | Запоминать имя, телефон, адрес; «Определить, где я» — отложить | После **успешного** заказа контакты сохраняются в localStorage этого телефона (`apetit.contact`) и в следующий раз подставляются сами. То, что человек уже начал вводить, не затирается; прочитанное проверяется теми же правилами, что и форма (битое — не подставляется). «Определить, где я» — в SPEC §11 как открытый вопрос. |
| 4 | «NR. 1001» / «№ 1001» подходит | Как есть. |
| 5 | Supabase + Telegram — следующая задача | Не трогал. |

### Точки

- **Florești убрана** с сайта: из списка городов и точек, с экрана городов
  (теперь 4 плитки), `/floresti` → 404. Сохранённый на телефоне город
  «floresti» больше не принимается — человек увидит экран городов; корзина
  Florești считается пустой. В SPEC §1.1 точка помечена «закрыта».
- **Адреса и координаты** 5 точек — в `points.ts` и в таблице SPEC §1.1.
  Sculeni — примечание про COFFEEIN (на сайте «Apetit Sculeni»).
- **Расстояние включено.** В Сороках при показе блока «Punctul» сайт один раз
  спрашивает геолокацию; разрешили — в карточке «~0,4 km», отказ — молча без
  расстояния (проверено тестами: с разрешением — «~0,4 km» / «~2,0 km»,
  без — ни «km», ни ошибок). В остальных городах блока нет — и вопроса нет.
  В карточках теперь настоящие адреса.

### Тексты

Все `[ТЕКСТ: …]` заменены на утверждённые (ro + ru), добавлена кнопка во
время отправки «Se trimite…» / «Отправляем…» — неактивна, без крутилки.
Пометки «на утверждение» и `TODO ru` у этих строк сняты. Новый тест: в
текстах сайта не осталось ни одной заглушки `[ТЕКСТ`.

### Проверки

Vitest — **193** (было 187): точки (5 точек, 4 города, у каждой адрес и
координаты в пределах Молдовы, «floresti» не принимается), хранение
контактов (сохранили/прочитали, мусор, неверное поле не подставляется,
хранилище бросает ошибку), нет заглушек в текстах. Playwright — **43** (было
37): новые — порядок блоков на телефоне, колонка справа на десктопе,
«Se trimite…» во время отправки, контакты подставляются после заказа
(и не сохраняются до него), расстояние с геолокацией и без, `/floresti` →
404, экран городов на 4 плитки; старые тесты — с настоящими текстами.
`npm run lint` ✓, `npm run build` ✓ (код выхода 0).

Безопасность (серверного кода почти не менял — только данные точек):
контакты хранятся только на телефоне человека и только после его заказа;
при чтении проверяются правилами формы, выводятся через React (без вставки
HTML). Новых входов на сервер нет.

**Скриншоты** (по списку): `docs/screens/01-orase-390.png`,
`05-comanda-390.png`, `05-comanda-1280.png` — с расстоянием в карточках
(в съёмке включена геолокация «человек в Сороках»).

### Что не сделано / отличия

- По списку переснял только три скриншота. **Устарели:**
  `01-orase-1280.png` (там ещё 5 городов), `05-comanda-inchis-390.png` и
  `05-confirmare-390.png` (старый порядок блоков и заглушки). Переснять —
  одна команда `npm run screens`.
- В SPEC Florești ещё упоминается в §0 (описание сети) и §8 (адреса
  страниц) — по заданию менял только §1.

### Вопросы архитектору

1. **Экран городов на десктопе**: плиток теперь 4, сетка по DESIGN — 3
   колонки, получается ряд из 3 и одна плитка под ним. Оставить или сделать
   2×2 (или 4 в ряд)? На телефоне всё в столбик — там порядок.
2. Убрать Florești из SPEC §0 и §8 и переснять устаревшие скриншоты?

## 2026-09-19 — Этап 5 — правовые страницы, согласие, подвал, защитные заголовки

### Что сделано

- **Две страницы: `/confidentialitate` и `/termeni`.** Тексты — дословно
  утверждённые (ro + ru), лежат в `src/i18n/legal.ts`. Язык выбирается как на
  всём сайте: по сохранённому городу (Otaci → ru, остальные и «город не выбран»
  → ro). Переключатель RO/RU в шапке здесь **работает** и меняет язык на месте.
  Русский текст для Otaci не мигает румынским: язык ставит крошечный скрипт
  ещё до отрисовки страницы (как у редиректа на город). Шапка как на сайте:
  APETIT, город (если выбран), RO/RU, без чипов и корзины. Заголовок Oswald,
  текст Montserrat 16px, строка до 640px, подзаголовки Manrope 700, без
  карточек и иконок. Внизу «Înapoi la meniu» / «Вернуться в меню» — в меню
  сохранённого города (или на экран городов, если город не выбран).
- **Строка согласия** под «Casierul te va suna pentru confirmare»: 13px, Smoke,
  «Termenii» → /termeni, «Politica de confidențialitate» → /confidentialitate,
  без галочки. Ссылки открываются **в новой вкладке**, чтобы человек не
  потерял заполненные имя и телефон.
- **Подвал меню**: после последней категории — линия 1px Ink, две строки
  13px Smoke, ссылки на обе страницы и e-mail (mailto). Реквизиты — в одном
  файле `src/data/company.ts`: поменять e-mail/адрес — только там, подвал и
  обе страницы возьмут новое значение сами (есть тест, что в текстах страниц
  реквизиты не вписаны руками). Когда в корзине что-то есть, под подвалом
  добавляется высота панели корзины — строки не закрываются.
- **Защитные заголовки** на всех адресах (`next.config.ts` →
  `src/lib/security-headers.ts`): HSTS, nosniff, Referrer-Policy,
  X-Frame-Options DENY, Permissions-Policy и CSP — ровно как в задании. Плюс
  отключил заголовок `X-Powered-By: Next.js` (нашёл при проверке
  owasp-security — лишняя подсказка взломщику, по правилу «найденное —
  исправить»).

### CSP: почему без nonce

Проверил по документации Next 16 (в node_modules и через context7): nonce
работает **только если каждая страница собирается на сервере при каждом
заходе**. Тогда меню перестало бы быть готовым заранее (SPEC §9.5), каждый
заход считался бы на Cloudflare — медленнее и дороже. Поэтому рабочий вариант:
`script-src 'self' 'unsafe-inline'`. Встроенные скрипты разрешены (без них
Next не оживит статическую страницу), но **скрипты с любых других сайтов
запрещены**, данные уходят только на наш сервер, страницу нельзя встроить в
чужой сайт. `style-src 'unsafe-inline'` — анимации и размеры вывесок задаются
через атрибут style. `'unsafe-eval'` — только в режиме разработки (так требует
React). Экспериментальный SRI Next не решает проблему встроенных скриптов —
не включал.

По дороге нашёл: библиотека zod «на пробу» вызывает eval и получает запрет
CSP (в консоли — нарушение на меню и оформлении). Включил её официальный
режим без eval (`jitless`, файл `src/lib/zod.ts`; весь код теперь берёт zod
оттуда — это сторожит тест). Правила проверки заказа не изменились.

### Проверка в браузере (production-сборка, CSP без послаблений)

Меню → лист блюда → корзина → оформление (Сороки, геолокация разрешена) →
заказ → подтверждение, плюс `/`, `/otaci` и обе правовые страницы:
**нарушений CSP — 0, ошибок в консоли — 0**. Шрифты (Oswald, Manrope,
Montserrat) загружены со своего сервера, фото первого экрана загружены,
анимация появления плиток при прокрутке работает (4 → 17 плиток), расстояние
«~0,4 km» показано, заказ прошёл.

### Тесты

Vitest — **210** (было 193): заголовки и CSP (включая то, что next.config
отдаёт их на все адреса), язык правовых страниц и скрипт «до отрисовки»
(в т.ч. мусор в хранилище), тексты ro/ru одинаково устроены, реквизиты из
company.ts, строка согласия собирается в утверждённую фразу, zod без eval.
Playwright — **55** (было 43): страницы на ro и ru, переключатель, Otaci →
ru, ссылки подвала и строки согласия ведут куда надо (включая новую вкладку),
заголовки в ответе на 4 адресах, заказ проходит с CSP без нарушений.
`npm run lint` ✓, `npm run build` ✓ (код выхода 0; все страницы по-прежнему
статические, новые — тоже).

Скиллы: ui-ux-pro-max (текст 16px, строка ≤640px, зона нажатия ссылок ≥24px),
web-design-guidelines (добавил выравнивание заголовков и `translate="no"` для
названия компании), owasp-security, differential-review (отчёт вне
репозитория: `%TEMP%\apetit-reports\DIFFERENTIAL_REVIEW_legal-headers.md`).
semgrep не запускал — это перед релизом.

**Скриншоты:** `docs/screens/06-confidentialitate-390.png`,
`06-footer-390.png`, `05-comanda-390.png` (со строкой согласия).

### Что не сделано / отличия

- **Заголовок вкладки** браузера на правовых страницах всегда румынский
  («Politica de confidențialitate — Apetit»), даже когда текст русский: Next 16
  сам управляет заголовком и перезаписывает подмену. Текст страницы при этом
  на нужном языке.
- В ru-подвале адрес — «г. Сорока, ул. Тирасполь 4» (взят из утверждённого
  ru-текста политики), в задании для ru дана только вторая строка.
- Заголовок страницы: DESIGN не задаёт размер для такого заголовка. Слово
  «КОНФИДЕНЦИАЛЬНОСТИ» при 36px шире экрана телефона, поэтому размер зависит
  от ширины (на 390px ≈ 33px, потолок 36px; десктоп — 44px, как города).
- В DESIGN.md нет раздела «Secondary link styles». Ссылки в подвале и строке
  согласия — цвет текста, подчёркнуты, при наведении Ink.
- Цвет Smoke на кремовом фоне — контраст 4,45 : 1, чуть ниже нормы 4,5 для
  мелкого текста. Взял Smoke, как в задании (и как в остальном сайте).

### На будущее (выкладка)

- HSTS с `preload`: **не** подавать apetit.md в список hstspreload.org, пока
  все поддомены не работают по HTTPS — это почти необратимо.
- На Cloudflare файлы, которые отдаются напрямую (`/_next/static`, `public/`),
  не проходят через next.config — на этапе выкладки добавить `public/_headers`
  (хотя бы nosniff).

### Вопросы архитектору

1. В SPEC §8 адрес политики — `/politica-de-confidentialitate`, в задаче —
   `/confidentialitate` (сделал по задаче). Обновить SPEC §8 (и добавить
   `/termeni`)?
2. Устроит ли, что заголовок вкладки на правовых страницах всегда на ro?

## 2026-09-19 — Этап 5 — фоновые линии бренда и анимация чипов

### Мелочи из прошлого отчёта

- SPEC §8: адрес политики теперь `/confidentialitate`.
- SPEC §11: записал «Название вкладки на правовых страницах всегда ro —
  принято; решится с русскими адресами /ru/».

### Фоновые линии

- **ffmpeg** не было — поставил бесплатно через winget (`Gyan.FFmpeg`,
  версия 9.0.1). В PATH он появится после перезапуска VS Code; до этого
  скрипт берёт путь из переменной `FFMPEG`.
- **Кадры.** Видео — 30 секунд, 4K, серые контурные линии («топография») на
  белом. Вытащил 8 кадров: 1, 4, 8, 12, 16, 20, 24, 28 с → `docs/bg-frames/`
  (папка в .gitignore). **Выбрал 8 с**: линии по всему кадру ровной
  плотности, без сгустков. На 20–28 с есть места, где десятки линий
  сходятся в плотную полосу — на телефоне это выглядит как серое пятно
  («муар»). Рамок, чёрных полос и обрезанных краёв нет ни на одном кадре.
- **Картинка** `public/img/bg/linii.webp`: только линии цвета Ash
  #A79E95 на прозрачном фоне, 1600×900, **54 КБ**. Скрипт
  `scripts/bg-lines.py`: `frames` вытаскивает кадры, `build <кадр>`
  делает картинку — можно повторить с любым другим кадром. Сначала была
  91 КБ; уменьшил (8 ступеней прозрачности вместо 256, сжатие без потерь):
  рядом на увеличении разницы не видно.
- **Слой** стоит на экране городов, в меню и на оформлении: неподвижный,
  на весь экран, за контентом, клики проходят сквозь, картинка — cover.
  При прокрутке едет вверх на 0.35 от прокрутки (Motion `useScroll`, меняется
  только transform, React в этом не участвует). При «уменьшить движение» —
  стоит на месте. Сквозь стекло шапки и панели корзины линии видны
  размытыми.
- **Как слой не заканчивается на длинном меню.** Меню на телефоне —
  примерно 15 000 px прокрутки, то есть слой проезжает около 6 экранов, а
  картинка одна. Поэтому в слое три экрана подряд: картинка, её зеркальная
  копия, снова картинка. Через каждые два экрана сдвиг начинается заново.
  Зеркало нужно, чтобы на стыке линии продолжались, а не обрывались.
- Кремовый фон теперь задаёт только `html`, `body` прозрачный — иначе он
  закрывал бы слой. На других страницах ничего не изменилось.
- **Сила линий** — одна переменная CSS `--bg-lines-opacity`, стоит B (0.8).
  Скриншоты: `docs/screens/07-fundal-A-390.png` (0.5),
  `07-fundal-B-390.png` (0.8), `07-fundal-C-390.png` (1.0),
  `07-fundal-B-1280.png`. Названия, цены (на жёлтом) и состав читаются на
  всех трёх. **На C** линия, которая проходит через граммовку («440 g»),
  почти сливается с цифрой: цвет граммовки (Smoke) и линий (Ash) близки.
  Прочитать можно, но на C линии уже спорят с мелким серым текстом. На B —
  не мешают.
- На телефоне cover показывает только середину картинки (примерно четверть
  ширины), поэтому линии там крупнее, чем на десктопе. Так и задумано у
  cover, просто предупреждаю.

### Анимация чипов

- При нажатии на чип иконка делает **один оборот** (360°) за 250 мс по
  кривой `--ease-out` из DESIGN. Одновременно чип заливается Ink и страница
  едет к категории. Повторов и бесконечного вращения нет; если нажать
  ещё раз, пока иконка крутится, оборот не начинается заново (иначе был бы
  рывок). Иконка вращается ровно вокруг своего центра — проверил.
- С клавиатуры: Enter и Space работают так же (Space раньше листал страницу).
- При «уменьшить движение» — только смена цвета.
- Сделано средствами самого браузера (Web Animations API), без новых
  библиотек.
- **Нашёл и исправил старую ошибку**: при «уменьшить движение» после
  нажатия на чип активным оставался предыдущий чип (Burgers → горел Menu).
  Причина — край предыдущей секции касался «линии активности» ровно на
  1 px. Сдвинул линию на 1 px. Без этого требование «чип становится
  активным» при reduced-motion не выполнялось.

### Lighthouse (мобильный, страница /soroca, production-сборка)

Каждый вариант мерил 2–3 раза, цифры стабильные.

| Вариант | Performance | Accessibility | Best Practices | SEO | LCP |
|---|---|---|---|---|---|
| **Итог (с фоном)** | **78–79** | 96 | 100 | 100 | 5,9–6,1 с |
| До задачи (без фона) | 80 | 96 | 100 | 100 | 5,5 с |
| Итог, но картинка фона заблокирована | 80 | — | — | — | 5,5 с |

**≥90 по Performance нет — и не было до этой задачи (80).** LCP (самое
крупное на первом экране) — слово-вывеска «KEBAB». На быстром компьютере
она рисуется за 0,2 с, но к этому моменту браузер уже запросил ~1,1 МБ:
шрифты 213 КБ (9 файлов), скрипты 261 КБ, фото блюд ~600 КБ (в том числе
фото ниже первого экрана — браузер берёт их заранее). Lighthouse
симулирует медленный телефон и считает, что вывеска ждёт всё это, —
отсюда 5,5 с. Это не связано с фоном, и чинить это я не стал — задача не
про это (см. вопрос 1).

Сколько стоил фон и что я сделал:
- сначала фон стоил −2…3 балла. Главной причиной оказалась не картинка,
  а скрипты: компонент `motion.div` тянул в меню ещё 44 КБ JavaScript.
  Убрал его: из Motion остался только `useScroll`, сдвиг пишется в
  transform напрямую. Теперь +10 КБ;
- картинку уменьшил с 91 до 54 КБ. На оценку это почти не повлияло:
  ощутимо стоит сам факт ещё одной загрузки на первом экране, а не её вес;
- сейчас фон стоит ~1 балл (LCP в симуляции +0,4–0,6 с). Эффект не убирал.

Accessibility 96: мелкий текст цвета Smoke — контраст 4,46 при норме 4,5
(это уже было в прошлом отчёте), с фоном не связано.

Lighthouse запускал через `npx lighthouse@13.4.1`, в проект он не добавлен.
Версию 13.5.0 брать не стал — она вышла вчера (свежий релиз — лишний риск),
13.4.1 в обращении два месяца, это официальный пакет Google Chrome.

### Проверки

- Vitest — **217** (было 210): сдвиг 0.35 и повтор через два экрана,
  картинка (WebP, 1600px, прозрачность, ≤150 КБ), CSS слоя (fixed, под
  контентом, клики сквозь, сила 0.8).
- Playwright — **63** (было 55): слой есть на `/`, `/soroca`,
  `/soroca/comanda` и не перехватывает клики; при прокрутке на 1000 px слой
  сдвигается на −350 px; при reduced-motion не двигается; чип — один оборот
  250 мс по нужной кривой, переход к категории и активный чип (мышь, Enter,
  Space); при reduced-motion оборота нет, переход работает.
- `npm run lint` ✓, `npm run build` ✓ (код выхода 0, все страницы
  по-прежнему статические).
- Скиллы: emil-design-eng, animate, review-animations, web-design-guidelines.
  Скилл review-animations есть в `.claude/skills/`, но сам не вызывается
  (стоит запрет на автозапуск) — прочитал его правила и прошёл по ним
  вручную: анимируется только transform, reduced-motion соблюдён, 250 мс,
  без повторов. web-design-guidelines — замечаний нет. ui-ux-pro-max
  отдельно не запускал — читаемость текста на фоне проверил по скриншотам.
  owasp-security и differential-review не нужны: серверный код не менялся.
  Новых npm-пакетов нет.

### Что не сделано / отличия

- **Во время плавной прокрутки заливка чипа «пробегает» по промежуточным
  категориям.** Так было и раньше. Пример: нажал Crispy → Crispy
  залился и сделал оборот → по дороге по очереди загораются Kebab, Menu,
  Burgers, Gözleme → снова Crispy. С анимацией это стало заметнее.
  Не чинил — это вне задачи (см. вопрос 2).
- **DESIGN.md расходится с этой задачей** — делал по задаче:
  - Background: в DESIGN линии Ink, картинка повторяется плиткой, файл
    `public/img/brand/linii-fundal.png`, requestAnimationFrame. В задаче —
    Ash, cover, `public/img/bg/linii.webp`, Motion `useScroll`.
  - Category Chips: в DESIGN 350–400 мс и выезд слова-вывески при нажатии;
    в задаче — 250 мс, только иконка.
  - В Do's: «Фон — ровный Cream везде… никаких текстур».
- По правилам Emil анимацию на действия с клавиатуры не делают вообще
  (пользователь повторяет их много раз, анимация кажется медленной). Сделал,
  как в задаче (Enter/Space — то же, что нажатие), но отмечаю.
- Вращение по правилам Emil — «движение на месте», для него советуют
  ease-in-out. Взял `--ease-out` из DESIGN, как сказано в задаче.

### Вопросы архитектору

1. Performance 78–79 (<90). Главная причина была и до фона: на первом
   экране меню сразу запрашивается ~1,1 МБ (шрифты, скрипты, фото, в том
   числе ниже экрана). Предлагаю отдельную задачу «скорость меню»:
   позже грузить фото ниже первого экрана и проверить, все ли 9 файлов
   шрифтов нужны сразу. Отдельно по фону: грузить его картинку после того,
   как страница «ожила», с мягким появлением — это вернёт ~1 балл.
   Делать что-то из этого?
2. Заливка чипа «пробегает» по категориям во время плавной прокрутки.
   Исправить: пока страница едет к выбранной категории, не менять
   активный чип?
3. Обновить в DESIGN.md разделы Background и Category Chips под эту
   задачу (Ash, cover, WebP, 250 мс, только иконка)?
