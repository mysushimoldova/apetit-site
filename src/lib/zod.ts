// zod для всего проекта — импортировать отсюда, а не из "zod".
// jitless: zod 4 проверяет, можно ли ускоряться через new Function (eval).
// Наш CSP eval запрещает (src/lib/security-headers.ts), и эта проверка
// давала в браузере нарушение CSP на каждой странице с формой/корзиной.
// Без ускорения проверка чуть медленнее — на наших маленьких данных незаметно.
import { z } from "zod";

z.config({ jitless: true });

export { z };
